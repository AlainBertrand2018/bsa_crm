import {genkit} from 'genkit';
// import {googleAI} from '@genkit-ai/googleai'; // Keep this commented out

export const ai = genkit({
  plugins: [], // Initialize with no plugins
  // model: 'googleai/gemini-2.0-flash', // Keep this commented out
});
